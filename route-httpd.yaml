apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: httpd
  name: httpd
  namespace: simple-httpd-as
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: httpd
